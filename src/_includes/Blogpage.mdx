---
layout: Blog.tsx
title: Blog
---

<div class="markdown">
  # Blog

  ---
</div>

<div>
  {page.data.results
    .map((page) => (
    <a href={page.url}>
      <div class="card my-4 text-black text-decoration-none">
        <div class="card-header">
          <div class="flex justify-between flex-wrap m-3">
            <h2 class="text-xl font-medium">{page.title}</h2>
            <span>
              {new Date(page.date).toLocaleDateString("de-DE", {
                year: "numeric",
                month: "long",
                day: "numeric",
              })}
            </span>
          </div>
        </div>
        <div class="card-body">
          {page.imgUrl ? (
            <img class="w-full" src={page.imgUrl} />
          ) : null}
          <div class="m-3">{page.excerpt}</div>
        </div>
      </div>
    </a>
  ))}
</div>

<nav>
  <ul class="pagination">
    {page.data.pagination.previous ? (
      <li class="page-item">
        <a class="page-link" href={`/blog/${page.data.pagination.previous}`}>
          &laquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link disabled">&laquo;</a>
      </li>
    )}
    {new Array(page.data.pagination.totalPages).fill().map((_, index) => (
      <li class="page-item">
        <a class="page-link" href={`/blog/page/${index + 1}`}>
          {index + 1}
        </a>
      </li>
    ))}
    {page.data.pagination.next ? (
      <li class="page-item">
        <a class="page-link" href={`/blog/${page.data.pagination.next}`}>
          &raquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link disabled">&raquo;</a>
      </li>
    )}
  </ul>
</nav>
